var GardenPuzzle = (function(){
    /**
     * Default options
     * @type Object
     */
    var defaultOptions = {
        domContainer: document.getElementById('garden-container')
    };
    
    /**
     * Class GardenPuzzle
     * @param {Object} options
     * @returns {new GardenPuzzle}
     */
    function GardenPuzzle(options) {
        this.options = populateDefaults(options, defaultOptions);
        
        _p.init.call(this);
    }
    
    /**
     * Puzzle options
     */
    GardenPuzzle.prototype.options = {};
    
    /**
     * Private methods
     * @type Object
     */
    var _p = {
        init: function() {
            
        }
    };
    
    return GardenPuzzle;
}());